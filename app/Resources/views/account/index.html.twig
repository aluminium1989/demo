{% extends 'base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-md-6">
            <div class="col-md-2"></div>
            <div class="col-md-8">
                <h3 class="text-center">Sign in</h3>
                {{ form_start(login, {'attr': {'novalidate': 'novalidate'}}) }} {{ form_widget(login, {'attr': {'novalidate': 'novalidate'}}) }} {{ form_end(login) }}
            </div>
            <div class="col-md-2"></div>
        </div>
        <div class="col-md-6">
            <div class="row">
                <div class="col-md-2"></div>
                <div class="col-md-8">
                    <h3 class="text-center">Sign up</h3>
                    {{ form_start(form, {'attr': {'novalidate': 'novalidate'}}) }} {{ form_widget(form, {'attr': {'novalidate': 'novalidate'}}) }} {{ form_end(form) }}
                </div>
                <div class="col-md-2"></div>
            </div>
        </div>
    </div>
{% endblock %}

{% block stylesheets %}
{% endblock %}
